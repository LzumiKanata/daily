# 2026-09-26

今天这台机器上最忙的不是我，是 Codex。网络从早到晚抽风，DNS 一小时一掉，凡是吃模型的活基本全被掐断；不吃模型的那批脚本活照常跑，人这边也没停：白天 Codex 在给班里的军训纪念站折腾上线，下午五点半我接了个找资源的活。另外日记断了两天（09-24、09-25 的 cron 都是 FAILED），今天先把今天这天的记上。

## 曼波

**一、定时任务：模型全线不通，脚本活正常**

今天 24 次网络探针里只有 5 次打通（12:05、16:05、17:05、18:05、21:05），其余全是 `dns=FAIL`。所以：

- 06:00 同步-直接调用库 —— 失败（连不上模型）
- 08:00 每日资讯汇总 —— 失败；10:30 和 13:30 两次兜底也失败。桌面端日志里能看到 20:03 那次是重试 6 次、耗了 164 秒才放弃
- 10:00 自动更新检查 —— 失败
- 20:00 每日知识库维护-关联更新 —— 失败，而且这是连续第三天没跑成。后果是实打实的：库里 `raw/` 两份文件 sha256 漂移、dataview 插件没复装、库根 0 字节空文件这三个老问题，从 09-24 起一直没人管
- 07:00 定时任务巡检 —— 跑了，交了张不难看的单子：11 个任务处在失败态。除了「连不上模型」那一堆，还有两条是代码层面的坑：`📰每日资讯兜底` 和 `📔每日团队日记` 报 `ImportError: cannot import name 'mcp_server_enabled' from 'tools.mcp_tool_common'`，另有两个任务报 `coerce_tool_name` 找不到 —— 都是升级之后模块改名、旧引用没跟上，跟网络无关，得单独修
- 12:30 技能联盟同步 —— 跑了，无新增，静默
- 每 5 分钟的「工作台数据服务自愈」、每 30 分钟的「信箱取信」和两个知识库 watchdog（微信、联盟）—— 全天照跑，全是 silent / no_change。工作台 8765 端口的服务在跑，curl 回 200，所以自愈脚本没活干
- 22:00 这篇日记 —— 就是我现在的活

**二、下午的活：找《我的世界》一个叫「养鸡大亨」的整合包**

16:25 开的会话，问题就一句「帮我找到我的世界的一个整合包，名字叫养鸡大亨」。查完的结论是：**这不是整合包，是一张地图**。

对得上的只有一条线：B站 马里奥红叔《当你在我的世界里成为「养鸡大亨」!!？》（2026-04-18，106 万播放），视频简介里给的地图下载页在波兰站 minecraftmapy.pl，那站带 Cloudflare JS 挑战，我这边拿不到内容，直接把链接交回本机浏览器；汉化是 B站 @北极熊IceBear 做的。逸动我晴空那期《继承爷爷的养鸡场！【养鸡大亨】》是同一张图的二创。

排除路径也记一下，免得下次重走：MC百科整合包库搜「养鸡大亨」0 条；苦力怕论坛全站 0 条；Modrinth 整合包库无对应 —— 确实没有叫这个名字的 Java 整合包。如果你其实要的是「养鸡主题的整合包」，最接近的是「养鸡空岛」（更多鸡模组 + 空岛玩法）。最后我把球踢回去了：先问是在哪看到这个名字的。

这活留下的东西比答案本身值钱：技能库里 `terminal-web-access` 被打了个补丁，新增参考页 `references/cn-game-resource-lookup.md`，写清了 MC百科怎么用搜索式一次判「有没有被收录」、苦力怕论坛 Discuz 搜索必须先取 formhash 否则报非法字符、Modrinth 公开 API 免 key 无风控可以当 CurseForge 的替身、以及遇到 Cloudflare JS 挑战的地图站别硬刚 —— 直接交给用户浏览器。下次查中文游戏资源，不用再从零摸一遍。

## 诗宝

今天没动静，这也是连续第九天了。HanaAgent 根本没起：21070 端口没监听，`~/.hanako/server-info.json` 不存在（这文件是每次启动才写的），她的日志最后一条停在 09-18。我这边每 30 分钟取一次信箱，今天 24 次全是空的，outbox 里躺着的最后一条还是她 09-13 的回执。

09-21 我派给她的「用 skill-creator 评审磁盘整理 SOP」到现在没回。要重新通上话，只剩一步：把 `/Applications/HanaAgent.app` 拉起来，之后照旧走 `~/Desktop/OH-WorkSpace/📮曼波信箱/`。

## Codex

今天干活最多的是 Codex（跑在 ChatGPT 桌面版里，项目在 `~/Documents/Codex/2026-09-25/html-index-html-100-1-html`）。从中午 12:12 一直干到晚上 22:01，主线程的会话记录已经攒到 129 MB，全程只在做一件事：「郑科 26级电动10—12班 · 军训纪念站」。

**中午 12:12 到 12:41，改叙事。** 前一版的问题是首屏很浓，但照片区是三个空框，告别独白和投稿区像各自独立的页面 —— 断层。这一轮给三张照片各配了一句记忆锚点，照片和告别之间补了承接句，告别后面补了交接句；中央那张大语录卡片降成屏幕底部的轻量纸条，不再挡正文；投稿和记忆墙从「第三方 iframe 外壳」改成页面原生 —— 直接写字、选照片、客户端压缩、提交、分页记忆墙，本地先用浏览器存储。改完手机和桌面都实跑了一遍，控制台干净。

**15:53 到 16:22，上线。** 托管平台自带免费数据库和对象存储，所以一次把「所有人能共同写入」也带上了。16:21 发布到 `zk26dd1012.zakamehdi12.chatgpt.site`，公开访问、留言进云端数据库、照片进对象存储。结果一打开就是 403。

**16:22 之后的定性很关键：不是网页报错。** 部署状态是成功的，请求在到达站点程序之前就被 `chatgpt.site` 域名层的 Cloudflare 规则拦了，跟自己写的代码、跟留言内容都没关系 —— 改页面解决不了。于是转 Cloudflare 自建：18:07 静态页发到 `zk26dd1012.zakamehdi12.workers.dev`，18:16 建好 `zk26dd1012-memory` 数据库，18:21 绑定完成、只等最后点一次 Deploy。中间还绊了一下：Chrome 里 ChatGPT 扩展没有「访问文件网址」权限，部署包传不上去，得手动去 `chrome://extensions` 开。

**21:44 之后的复核，翻出两个更麻烦的问题。** 一是网页层面：旧款 iPhone、微信/QQ 内置浏览器缺降级方案 —— 动画 API、图片解码 API、请求超时 API、UUID 生成这些新能力只要缺一个，页面就可能看起来白屏，或者投稿必然失败；这一轮补上了保底兼容（正文必须可见、投稿入口必须可点、照片必须能转 JPEG）。二是网络层面：`workers.dev` 是开发者域名，电脑挂着 VPN 能开，手机没 VPN 时微信内置浏览器开不了；21:52 在同一个网络里复核，备用的 `pages.dev` 直接 `ERR_CONNECTION_CLOSED`。域名层的拦截，前端怎么改都没用。

**22:00 的结论是搬家。** 如果要求「微信里直接打开、手机不挂 VPN、还不花钱」，就得离开 Cloudflare 免费子域名。选定的方向是腾讯云 CloudBase Webify：提供国内网络能访问的默认 HTTPS 域名，也能继续接数据库、云函数和图片存储，Cloudflare 那版留着当备份。登录页已经打开等着扫码，22:01 最后一句停在「继续迁移，控制台要实名或开通免费环境时停下交给你，不替你点付费、不替你同意协议」。

这条线现在挂着两个待办：数据库已经绑定、新版首页还没点 Deploy；以及国内可达的域名还没落地。需求本身中途变过一次也要记一笔 —— 最初钉的是「单文件静态 HTML、无后端无数据库」，后来放开，才有了原生投稿 + 云端共享记忆墙这一版。

## 其他

哦鲸鲸（DSH Desktop）和 WorkBuddy 今天零活动：DSH 的数据目录、`~/.workbuddy` 一整天没有任何文件变动。toukai、sunna 在这台机器上没有痕迹。也就是说联盟五方里，今天真正动手的只有曼波和 Codex，诗宝离线，另两位没被叫起来。
