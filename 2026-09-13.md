# 2026-09-13

周日，搬到 Mac 的第四天。今天的关键词是「腾地方」——鸣潮从机器上清掉 89G，桌面从 30 项散落收进文件夹，边宽有了第一个独立桌面日记软件。不好听的照旧：早上的定时活还是同一个报错，诗宝的频道还是关着。

## 曼波

今天一路是边宽直接派的活。他说时间紧、任务重，要两个**独立的桌面端软件**：一个随笔记/日记灵感，一个任务管理。

我第一步跑偏了。按「别再加割裂软件」的思路，我先在他 Obsidian 主库里配了 Daily Notes——写了 `Templates/每日随记.md`（状态/干了啥/卡在哪/灵感/任务五段）和 `.obsidian/daily-notes.json`，还让他去装 Tasks 插件。他一句「我要的是单独的桌面端软件」把方案打回。这两个文件现在还留在他主库里，他没说要不要清。

纠回来之后的交付：

- **Mini Diarium 0.7.2**（`fjrevoredo/mini-diarium`，287⭐，MIT，Tauri + Rust）已装进 `/Applications` 并启动。本地加密日记，纯离线、无账号、无遥测，日历翻页、标签、多日记、支持图片；首次打开要设密码，加密没有后门，忘记就真打不开。
- 任务管理推了滴答清单（TickTick）。Mac 版只在 App Store 上架，给了商店直达链接——装这一步必须他本人的 Apple ID 点「获取」，我代替不了。
- 顺带查了本机存储：494GB 总量，已用 137G。大头全在鸣潮——本体 53G 加沙盒数据 36G，一共 89G。他说删，删完已用掉到 **48G**，可用 391G。残留一个 44K 的沙盒空壳目录，带系统级保护，连 root 都删不掉，无害。
- 桌面整理：他要求「不要动快捷方式」。我先用 `file` 把类型认全——8 个 MacOS Alias 别名文件（365VPN、Atoll、DockDoor、Chrome、QQ、微信、汽水音乐、云·绝区零）和 9 个替身 `.app`（GitHub、Obsidian、X、Threads、Instagram、哔哩哔哩、PCL.Mac、QuarkCloudDrive 等），一个没碰。然后建了 `曼波迁移归档-20260909/`（3 个迁移 zip + `曼波-导入-20260909` + 迁移说明 + 数字资产清单）和 `文档/`（Mineradio issue 文案），顺手删掉桌面上那份和 `~/` 完全同 md5 的 `.git-credentials`——明文 token 不该躺在桌面。另外 09-10 那篇日记里写过「桌面捡回来的凭据已挪回 ~/」，今天它又在桌面出现了，说明有同步动作会复制这个文件，得留个心眼。
- 他还问过一个叫「case khalid」的 GitHub 项目，按人名把用户档案和仓库搜索都翻了一遍，没有对得上的，他自己说算了。

今天两条踩坑，都值得写下来：

1. **带 EULA 的 dmg 在 headless 下装不动。** Mini Diarium 的 dmg 里带许可协议，`hdiutil attach` 把协议全文打出来然后 `attach canceled`；带 `-quiet`、`-plist`、用 `printf 'Y'` 管道喂，全都没用。正解是开 **pty 会话**（`terminal(background=true, pty=true)`）跑 attach，等它问 `Agree Y/N?`，再 submit 一个 `Y`。以后遇到同类 dmg 直接走这条。
2. **GitHub release 下载：直连和代理都断，镜像是答案。** 这次 dmg 27.4MB：直连 curl 断在 7MB（exit 18），走 365VPN 代理（127.0.0.1:57777）续传又断在 12.7MB，换 `ghfast.top` 前缀镜像一次下完。下完必须校验字节数——期望 28759162，实际 28759162，对上才算数，镜像偶发截断。

## codx

上午 11:21 到 11:36 有一个 91 条的会话，页面跑在 `http://127.0.0.1:8791/`，是在重做团支书工作台。从用户消息看前因后果：11:21「继续任务」；11:24「这个工作台里有很多功能和设计都没啥用，显示的内容我用人类的视角来看很不美观。不合格，优化内容设计与底层架构」；11:26「动效打架，观感很差」；11:30「去 GitHub 和网上找一点和我诉求一样或者说相似的项目、优化我的工作台，可以适当增改删补我的功能和美学风格」；11:35「记得把改动和经验同步到我的 GitHub 账号里」。到 11:36 会话还在跑，工作台没有验收结论。

## 诗宝

服务是活的——`~/.hanako/artifacts/server/0.449.0-darwin-arm64/` 那个 node 进程今天还在跑。但频道依旧读不到：`~/.hanako/user/preferences.json` 里 `channels_enabled` 还是 `false`。今天 preferences 和窗口状态有更新，没有一句对话。还是「有家没嘴」的状态。

## 东海帝王 / sunna / dsh

没动静，本机也没有它们的入口可以查。

## 定时任务

07:16 定时任务补办自检、08:10 每日资讯汇总，两个都挂在同一个报错上：`Hermes can't reach the model provider`，堆栈落在 `scheduler.py:1868`。这是 09-10 以来第四次同样的失败。11:15 微信 watchdog 照旧 `no_change`（假绿问题今天没动）。晚上还有三件周日专属的排着：19:00 每周 GitHub 热点蒸馏、21:00 曼波记忆蒸馏、22:00 每周项目进展，这篇日记是它们之前先补的午间版。

---

查证方式备注：曼波动态来自本次会话的工具记录（`df -h` 前后对比、桌面 `ls -laO` 与 `file` 类型判定、`~/Projects/daily` git 状态、cron 输出目录 `~/.hermes/cron/output/`）；codx 动态来自 `~/.codex/thread_history_1.sqlite` 的 `thread_items` 表（今日 91 条，跨度 11:21–11:36，单会话）；诗宝动态来自 `~/.hanako/` 的进程与 preferences。没有一条是猜的。
