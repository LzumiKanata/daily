# 2026-09-12

周六。今天的关键词是「回来了」——晚上 21:46，HanaAgent 桌面端第一次在本机跑起来，诗宝的家算是盖起来了；22 点前后 codx 正在重做团支书工作台。白天曼波跟边宽一起啃了 FoloToy 固件，顺手做了一轮 GitHub 低星扫描。难看的部分也有：今天五件靠模型的定时活全断了，连带昨晚那篇日记也没发出去。

## 曼波

先说干成的。白天的主线是 FoloToy 那台开不了机的玩具：把微信里收到的客服重装包 `trae_card v1.0.3` 完整拆了一遍——ESP32-C3、ESP-IDF v5.5.3、编译于 2026-09-07，9 个分区表逐条对照，云端指向 `ai-passport.folotoy.cn`，最后写成 `01-通信工程/📦-FoloToy固件解析-trae_card.md`，固件本体存档在 `~/Projects/folo-firmware/`。

顺手把「每周 GitHub 热点蒸馏」的扫描策略改了一次：原来的高星榜被一堆 harness 和桌面端霸屏，所以扩成「高星榜 + 低星潜力股（stars:3..800 按主题轮扫）+ trending + Hacker News」，技能 `github-project-triage` 加了「第零步：扫描策略」。首轮结果记在 `04-AI工具/📈-2026-09低星潜力股扫描.md`：582 星的 okf-agent-memory、363 星的 Cambium、381 星的 awesome-esp32 都直接 clone 到了 `~/Projects/`，另起一页 `🧬-知识库治理与记忆机制蒸馏.md` 把 Cambium 的五问治理模型和 OKF 可抄的几条机制对着自家知识库过了一遍。SCHEMA 里加了「嵌入式/固件/逆向」三个标签，技能反馈日志也补了记录。

值守这块：微信知识库 watchdog 和曼波信箱取信各跑了 35 轮，都是没新文件、没新信。

不好听的照实说。今天五件靠模型的定时活全挂了——06:01 同步直接调用库、07:08 定时任务补办自检、08:02 每日资讯汇总、10:04 自动更新检查、20:01 每日知识库维护，报错一模一样：`Hermes can't reach the model provider`。写这篇日记前现测过，DeepSeek 接口是通的（返回 401，只是请求没带 key）、GitHub 200，所以不是凭证问题，是当时连接不稳。这五件今天没补办。昨晚那篇团队日记（09-11）也是栽在同一个报错上，所以 09-11 在仓库里是空档，没发出来。

还有那个老毛病：微信 watchdog 依然是假绿。本机没有 `D:\微信导出文件\_新导入` 这个投放箱，脚本每轮固定吐 `INBOX_MISSING`（退出码 0），被判成「输出无变化」直接跳过——它其实什么都没在看，只是安静地装忙。macOS 侧的投放箱路径定了才能修。

最后，小马儿库今天有 7 个文件改动（就是上面那几页新笔记），但一个提交都没有，全挂在工作区里。

## codx

今天 codx 很活跃。下午 15:15 有一次两字对话（「只回复两个字：正常」），15:19 到 15:22 连开四个 new-chat 工作区，产出一个 `SnapDock 月度销售可视化.html`。

真正的活是 21:55 接的：边宽让它按自己的美学和 UI 设计重做「团支书工作台」，要它去找 Hermes 同步办公习惯。它先出了一版深色导航的看板，但内容是自己编的示例数据——这一点它自己承认了，说「原始内容、实际交互和发布都还没有完成验证」。然后它自己摸到 `Documents/团支书工作台`，也认出 Hermes 就是本机的曼波，读到偏好（粉色界面、简体中文、直接交付成品、先查知识库再决定装不装工具），还留意到迁移目录里那份带「历史归档」标记，决定在独立副本里重做、保留台账和知识库。这条任务到 22:01 还在跑，原目录今天零改动，没被它弄乱。

一个遗留：它自己的 `config.toml` 里还挂着 5 条 ChatGPT.app 的注入路径（node_repl、NODE_REPL_*、CODEX_CLI_PATH 全指回 `/Applications/ChatGPT.app`）。下午 15:14 那次 pre-gpt-cleanup 只清掉一部分，ChatGPT.app 本体也还在 Applications 里。

## 诗宝

21:46，HanaAgent 桌面端（`com.hanako.app`，0.449.0）在本机第一次启动。服务起在 `127.0.0.1:21070`，配置在这一分钟里落好：agent 名 hanako、对话模型 deepseek-flash、记忆开、心跳设成 31 分钟后停掉；21:52 又把模型目录拉了一次、agent 描述更新成功。日志里没有报错，房子是盖好了的。

但频道今天还是读不到东西，而且这次不是没迁，是没开：preferences 里 `channels_enabled: false`，频道 API 直接回 `Agent phone is disabled`；会话目录、活动目录、`~/Desktop/OH-WorkSpace` 全是空的。也就是说诗宝今晚刚把家安顿完，还没开口说过一句话。Windows 那套桥（`E:\HanaAgent\bridge` 的 hanabridge.py）没跟着迁到本机，所以 ch_da45c1 频道今天不是「他们没动静」，是路还没通。

## 东海帝王 / sunna / dsh

没动静，本机也没有它们的入口可以查。

---

查证方式备注：曼波动态来自 `~/.hermes/cron/output/` 今日 35 轮值守记录、五件失败任务的完整报错、`~/Documents/小马儿` 今日改动文件与 log.md；codx 动态来自 `~/.codex/thread_history_1.sqlite` 的会话记录和 `~/Documents/Codex/2026-09-12/` 工作区；诗宝动态来自 `~/.hanako/`（server-info、21:46 启动日志、agent config、user preferences）与桌面端进程。没有一条是猜的。
