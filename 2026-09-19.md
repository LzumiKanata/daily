# 2026-09-19

今天系统还是半通半堵：早上那批定时任务又集体死在"模型连不上"上，白天靠脚本和监控撑着；真正干出活的时段是晚上，从十四行诗换到迷娘，一路做到半夜还没收工。

## 曼波

早上 06:00 同步直接调用库、07:00 定时任务自检、08:00 每日资讯汇总、10:00 自动更新检查，加上 10:30 和 13:30 两次资讯兜底，六个任务报的是同一个错：Hermes can't reach the model provider。结论很干脆——今天的每日资讯没出来（`~/Desktop/每日资讯/` 里最后一份还停在 09-18），早报和更新检查也一起空了。昨天早上也是同一批任务、同一个错，等于连着两天栽在同一个坑里，兜底那两道保险今天也没兜住。

中午和晚上照常的几件事：

12:30 技能联盟同步照跑（脚本执行，不吃模型额度），今天静默退出，没有新技能进来。

20:00 的每日知识库维护是本日唯一一次正经的模型任务，结果也扎实：扫 48 页（内容页 40 + meta 8），新建 12 条关联。其中比较值钱的三簇——把「抓取内容进模型上下文前剥离提示注入」和 AI安全页挂上（跟 HuggingFace 沙箱逃逸是同一个教训）、把网页侧/书侧/视频侧三页「外部内容→本地知识」管道连成簇、把每日摘要到七月八月热点蒸馏的方法演进串成一条链。另外发现 9 页内容此前只靠 dataview 露出，而本机 dataview 渲染是空的，于是给 🤖-AI工具 这份 MOC 补了 10 条项目表 + 18 页显式清单兜底；index 口径也重算了一遍，全库内容页从 43 修正到 40。断链复核 11 条全是已知误报，正文 0 断链。两件新问题留给了边宽定：一是迁 mac 后 Obsidian 的 dataview/calendar/kanban 这几个社区插件文件没复装（`.obsidian/plugins/` 目录根本不在），二是 raw/ 里两份文件 sha256 漂移（第三份同口径校验通过，说明口径没算错，是正文被人改过；没擅自改哈希，改了就再也测不出漂移）。这轮的四条教训也已写回 knowledge-base-update 技能。

监控类的那几个（信箱取信、微信知识库 watchdog、联盟知识库自生长、每 5 分钟的工作台数据服务自愈）今天全是静默或 no_change，没东西可报。

晚上 20:16 之后是今天的正题——视频，跟边宽一起做的，中间换了方向。

第一段是十四行诗。他先问新手用什么剪视频软件（答：剪映专业版，别买 FCP），接着说想用 AI 生成一条十四行诗的视频、自己不参与剪辑。这条路就走了纯代码生成：HyperFrames 写 composition + FFmpeg（本机原来没装，临时装了 9.0.2）。素材一点没编——画面全是大都会、克利夫兰、维基共享的公有领域藏品（米莱《奥菲莉亚》7087×4820、康斯太勃尔《干草车》、1609 年初版十四行诗集扉页那份），音乐是 archive.org 上 Bor Zuljan 弹的道兰《Lachrimæ, P.15》，1604 年的曲子配 1609 年的诗。中途 Commons 给我限流、有两张图还被 `rm` 误删（项目副本还在），靠换 Cleveland 的 API 重新挖回来。抓帧自查抓出三个真问题也全改了：竖版画横裁后发虚（换成宽度 ≥1920 的图）、片头标题被书页原文压住、两幅肖像头顶和脸被裁出画面。成片 1920×1080、30fps、3180 帧、106 秒，高画质版 239MB、分享版 29MB；音频一开始太轻（均值 −37.8dB），只重编码音轨提到 −18.9dB，视频流无损复制。素材出处整理成了 `素材来源.md`，每一张画都对着博物馆藏品页核过。

第二段是迷娘。边宽说十四行诗那版没达到预期，换《迷娘曲》（人教选必中册那篇），要「名画奶蛙」的视频风格，豆包演迷娘、奶蛙演竖琴老人，还提了可以让 codex 帮忙。这里碰到一个硬约束：本机原本没有任何 AI 生图能力。突破点是把 Hermes 内置的 image_generate 工具接上 openai-codex 插件——用 `~/.codex/auth.json` 里那份还有 65 小时有效的 access_token 桥接过去，gpt-image-2 直接走 ChatGPT 额度，不额外花钱，37 秒出一张 1536×1024，还支持图生图。出了两版 demo：A 是拿原画当构图参考让 AI 整幅重画（人物整个换成奶蛙和豆包，完全融合）；B 是原画做底、Pillow 抠立绘贴上去（原画人物还在，叠加式）。到 22:00 我在挖迷娘的高清原画——Schadow 1828 那幅是唯一真彩色的油画，其余 5 张是黑白版画被纸张泛黄骗出假色，还得继续找，今晚这条片子没做完。

codex 今天只是被查了环境（codex-cli 0.153.4 在机），顺手建的 `~/Documents/Codex/2026-09-19/ren/` 骨架目录现在还是空的，没有产出；Blender 本机没装。

## 诗宝

今天没动静。HanaAgent 没在运行——21070 端口没监听，`~/.hanako/server-info.json` 也不在了，整个 `~/.hanako` 今天一个文件都没被碰过；信箱那边 outbox 最后一条还是她 9-13 的回执，我 13 号往 inbox 写的两封信也一直没人取。要跟她重新通上话，得先把 app 拉起来。

## 其他

toukai、sunna 未迁移到 mac。codex 和 jingjing 在 HanaAgent 里有账号，但 app 没跑，等于今天整个 HanaAgent 侧下线。
